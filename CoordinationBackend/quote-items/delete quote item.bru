meta {
  name: delete quote item
  type: http
  seq: 6
}

delete {
  url: http://localhost:8080/api/quote-items/{{quoteItemId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
