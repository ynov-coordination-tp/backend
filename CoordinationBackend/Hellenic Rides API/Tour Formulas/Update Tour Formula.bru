meta {
  name: Update Tour Formula
  type: http
  seq: 5
}

put {
  url: http://localhost:8080/api/tour-formulas/{{tourFormulaId}}
  body: json
  auth: inherit
}

body:json {
  {
    "tour": { "id": 1 },
    "formula": { "id": 1 },
    "isActive": false
  }
}

settings {
  encodeUrl: true
}
