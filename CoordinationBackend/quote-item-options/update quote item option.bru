meta {
  name: update quote item option
  type: http
  seq: 5
}

put {
  url: http://localhost:8080/api/quote-item-options/{{quoteItemOptionId}}
  body: json
  auth: inherit
}

body:json {
  {
    "quoteItemId": {{quoteItemId}},
    "optionId": 1,
    "quantity": 2,
    "lockedPrice": 100.00
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
