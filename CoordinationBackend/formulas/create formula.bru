meta {
  name: create formula
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/formulas
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Hermes",
    "includesMoto": true,
    "includesAccommodation": false,
    "includesMeals": false
  }
}

script:post-response {
  bru.setEnvVar("formulaId", res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
